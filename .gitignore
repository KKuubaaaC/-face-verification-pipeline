__pycache__/
*.pyc
.DS_Store
.venv/
*.egg-info/
dist/
build/
.ipynb_checkpoints/
mlruns/
*.onnx
models/*.pt
models/*.onnx
.ruff_cache/



# Optional: keep all of third_party out of Git (e.g. local SwinFace). Fresh clones
# without this directory → run: bash scripts/bootstrap_swinface.sh
third_party

# Dataset images & protocols (large / license — obtain separately; see README Data section)
data/
models/
space-main/models/
# Large embedding caches (regenerate via notebooks)
results/*.npz

# PDF files
*.pdf